# 发现与决策
<!-- 
  内容：任务的知识库，存储所有发现与决策。
  目的：上下文窗口有限，此文件是“外部记忆”，可持久保存且不受限制。
  时机：任何发现后都要更新，尤其是在完成 2 次 view/browser/search 操作后（两次操作规则）。
-->

## 需求
<!-- 
  内容：用户需求拆解成具体条目。
  目的：让需求始终可见，避免遗忘。
  时机：在阶段 1（需求与发现）填写。
  例子：
    - 命令行界面
    - 新增任务
    - 列出所有任务
    - 删除任务
    - Python 实现
-->
<!-- 从用户请求中整理 -->
-

## 研究发现
<!-- 
  内容：来自网页搜索、文档阅读或探索的关键发现。
  目的：多模态内容（图片、浏览器结果）不会持久存在，必须立即记录。
  时机：每完成 2 次 view/browser/search 操作后更新本节（两次操作规则）。
  例子：
    - Python 的 argparse 支持子命令，便于 CLI 设计
    - JSON 模块可轻松处理文件持久化
    - 标准模式：python script.py <command> [args]
-->
<!-- 探索过程中的关键发现 -->
-

## 技术决策
<!-- 
  内容：已做的架构与实现选择及其理由。
  目的：防止忘记为何选用某项技术或方案。
  时机：做出重要技术选择时更新。
  例子：
    | 使用 JSON 存储 | 简单、可读、Python 内置支持 |
    | argparse 子命令 | 清晰 CLI：python todo.py add "task" |
-->
<!-- 已做决策及理由 -->
| 决策 | 理由 |
|----------|-----------|
|          |           |

## 遇到的问题
<!-- 
  内容：遇到的阻碍及解决方式。
  目的：类似 task_plan.md 的错误记录，但更关注宏观问题（不局限代码错误）。
  时机：遇到阻塞或意外挑战时记录。
  例子：
    | 空文件导致 JSONDecodeError | 在 json.load() 前增加空文件检查 |
-->
<!-- 问题与解决方式 -->
| 问题 | 解决方式 |
|-------|------------|
|       |            |

## 资源
<!-- 
  内容：有用的 URL、文件路径、API 参考、文档链接。
  目的：方便后续查阅，避免丢失重要链接。
  时机：发现有用资源时添加。
  例子：
    - Python argparse 文档：https://docs.python.org/3/library/argparse.html
    - 项目结构：src/main.py, src/utils.py
-->
<!-- URL、文件路径、API 参考 -->
-

## 视觉/浏览发现
<!-- 
  内容：从图片、PDF 或浏览器结果中获得的信息。
  目的：关键！视觉/多模态内容不会持久存在，必须转成文本保存。
  时机：查看图片或浏览器结果后立即记录，不要等待。
  例子：
    - 截图显示登录表单包含邮箱与密码字段
    - 浏览器显示 API 返回 JSON，包含 status 与 data 键
-->
<!-- 关键：每完成 2 次 view/browser 操作后更新 -->
<!-- 多模态内容必须立即以文本记录 -->
-

---
<!-- 
  提醒：两次操作规则
  每完成 2 次 view/browser/search 操作后，必须更新此文件。
  这可以防止上下文重置导致视觉信息丢失。
-->
*每完成 2 次 view/browser/search 操作后更新此文件*
*这可以防止视觉信息在上下文重置时丢失*
